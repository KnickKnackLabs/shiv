#!/usr/bin/env bash
#MISE description="List available logo renderers"
set -eo pipefail

REPO_DIR="${MISE_CONFIG_ROOT:-$(cd "$(dirname "$0")/../../.." && pwd)}"
export PYTHONPATH="$REPO_DIR/lib"

python3 -m logo.cli renderers
